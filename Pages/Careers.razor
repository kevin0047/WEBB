@page "/careers"
@inject NavigationManager NavigationManager

<h1>채용 정보</h1>

<section class="company-culture">
    <h2>우리의 문화</h2>
    <p>
        [회사명]에서는 혁신, 협력, 그리고 지속적인 성장을 중요하게 생각합니다. 
        우리는 다양성을 존중하며, 모든 직원들이 자신의 잠재력을 최대한 발휘할 수 있는 환경을 제공합니다.
    </p>
</section>

<section class="benefits">
    <h2>복리후생</h2>
    <ul>
        <li>경쟁력 있는 급여</li>
        <li>유연한 근무 시간</li>
        <li>지속적인 교육 및 개발 기회</li>
        <li>건강 보험 및 웰니스 프로그램</li>
        <li>주식 옵션</li>
        <!-- 필요에 따라 추가 -->
    </ul>
</section>

<section class="open-positions">
    <h2>현재 채용 중인 포지션</h2>
    @foreach (var position in OpenPositions)
    {
            <div class="job-listing">
                <h3>@position.Title</h3>
                <p>부서: @position.Department</p>
                <p>위치: @position.Location</p>
                <p>@position.ShortDescription</p>
                <button class="apply-button" @onclick="() => ApplyForJob(position.Id)">지원하기</button>
            </div>
    }
</section>

<section class="application-process">
    <h2>지원 절차</h2>
    <ol>
        <li>관심 있는 포지션에 온라인으로 지원</li>
        <li>서류 검토 (1-2주 소요)</li>
        <li>전화 인터뷰 (30분)</li>
        <li>기술 평가 또는 과제 (해당되는 경우)</li>
        <li>대면 또는 화상 인터뷰 (2-3시간)</li>
        <li>최종 결정 및 오퍼</li>
    </ol>
</section>

@code {
    private List<JobPosition> OpenPositions = new List<JobPosition>
    {
        new JobPosition { Id = 1, Title = "소프트웨어 엔지니어", Department = "개발팀", Location = "서울", ShortDescription = "웹 애플리케이션 개발 및 유지보수" },
        new JobPosition { Id = 2, Title = "UX/UI 디자이너", Department = "디자인팀", Location = "부산", ShortDescription = "사용자 중심의 인터페이스 디자인" },
        new JobPosition { Id = 3, Title = "마케팅 매니저", Department = "마케팅팀", Location = "대전", ShortDescription = "디지털 마케팅 전략 수립 및 실행" },
        // 필요에 따라 추가 포지션
    };

    private void ApplyForJob(int jobId)
    {
        // 실제 구현에서는 지원 페이지나 폼으로 리디렉션
        NavigationManager.NavigateTo($"/apply/{jobId}");
    }

    private class JobPosition
    {
        public int Id { get; set; }
        public string Title { get; set; }
        public string Department { get; set; }
        public string Location { get; set; }
        public string ShortDescription { get; set; }
    }
}

<style>
    .job-listing {
        border: 1px solid #ddd;
        padding: 1rem;
        margin-bottom: 1rem;
        border-radius: 5px;
    }

    .apply-button {
        background-color: #007bff;
        color: white;
        border: none;
        padding: 0.5rem 1rem;
        border-radius: 5px;
        cursor: pointer;
    }

    .apply-button:hover {
        background-color: #0056b3;
    }

    .benefits ul, .application-process ol {
        padding-left: 1.5rem;
    }
</style>